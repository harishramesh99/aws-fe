version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - npm ci

  build:
    commands:
      - npm run build

 post_build:
    commands:
      - echo Build completed on `date`
      - echo doing nothing


artifacts:
  files:
    - '**/*'
 discard-paths: yes